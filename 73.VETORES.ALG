algoritmo "73.VETORES"
var
   v: vetor[0..9] de inteiro
   n, x, i:inteiro
inicio
      n <- 10
      para i <- 0 ate 9 faca
           n <- n - 1
           v[i] <- n
           escreva(v[i])
      fimpara

fimalgoritmo