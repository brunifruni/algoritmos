algoritmo "salariocomaumento"
var
   s, a, ns: real

inicio
      escreva ("Digite o salário do funcionário: R$")
      leia (s)
      ns <- (1.15*s)
      escreva ("O novo salário do funcionário com o aumento é de: R$", ns)

fimalgoritmo
